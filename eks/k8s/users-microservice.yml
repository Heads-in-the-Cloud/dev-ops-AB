apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-microservice
  namespace: default
  labels:
    app: users-microservice
spec:
  replicas: 2
  selector:
    matchLabels:
      app: users-microservice
  template:
    metadata:
      labels:
        app: users-microservice
      annotations:
        fluentbit.io/parser: crio
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/arch
                operator: In
                values:
                - amd64
                - arm64
      containers:
      - name: users-microservice
        image: $ECR_PREFIX-users-microservice:latest
        imagePullPolicy: Always
        env:
          - name: DB_URL
            valueFrom:
              secretKeyRef:
                key: db-url
                name: db-info
          - name: DB_USERNAME
            valueFrom:
              secretKeyRef:
                key: db-user
                name: db-info
          - name: DB_PASSWORD
            valueFrom:
              secretKeyRef:
                key: db-password
                name: db-info
          - name: JWT_SECRET
            valueFrom:
              secretKeyRef:
                key: value
                name: jwt-key
        ports:
          - containerPort: 8080

---

apiVersion: v1
kind: Service
metadata:
  name: users-microservice
  namespace: default
  labels:
    app: users-microservice
  annotations:
    alb.ingress.kubernetes.io/target-type: ip
spec:
  selector:
    app: users-microservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http
